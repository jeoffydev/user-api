import { RegisterViewModel } from './register-view-model';

describe('RegisterViewModel', () => {
  it('should create an instance', () => {
    expect(new RegisterViewModel()).toBeTruthy();
  });
});
